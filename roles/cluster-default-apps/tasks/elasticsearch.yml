- name: copy over elasticsearch files
  copy:
    src: elasticsearch/
    dest: /tmp/elasticsearch/

# deploy elasticsearch crd
- name: kubectl apply /tmp/elasticsearch/elastic-crd.yml
  shell: "kubectl apply -f /tmp/elasticsearch/elastic-crd.yml"

# deploy kibana crd
- name: kubectl apply /tmp/elasticsearch/kibana-crd.yml
  shell: "kubectl apply -f /tmp/elasticsearch/kibana-crd.yml"

# deploy kibana ingress
- name: kubectl apply /tmp/elasticsearch/kibana-ingress.yml
  shell: "kubectl apply -f /tmp/elasticsearch/kibana-ingress.yml"